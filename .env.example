# Thought2Markdown - Self-Contained Version
# This app requires NO external API or API keys.
# It uses a built-in local converter to transform your text into Markdown.

# APP_URL: The URL where this applet is hosted (for self-referential links)
APP_URL="MY_APP_URL"